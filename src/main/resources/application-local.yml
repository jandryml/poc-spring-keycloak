spring:
  application:
    name: poc-spring-keycloak
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/poc-realm
          jwk-set-uri: http://localhost:8081/realms/poc-realm/protocol/openid-connect/certs